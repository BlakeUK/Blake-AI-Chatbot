Screenshots for README.md.

Current (as of this update): 01-widget-greeting.png, 02-widget-conversation.png,
03-widget-tracking.png, 04-widget-mobile.png — all real screenshots of the live
widget taken via a headless browser against production (chat.blakegroup.uk),
using a local test harness page (not blake-uk.com itself, which doesn't have
the embed script added yet) so the widget's own network calls hit the real API.

The admin panel and operator console screenshots that used to live here
(admin-dashboard.png, admin-tickets.png, etc.) were removed in this update -
they were an early prototype/mockup, not a screenshot of the actual current
system (different colour scheme, missing most current tabs and features
entirely). Replacing them needs an authenticated admin session. See the
README's Screenshots section for what's current.
